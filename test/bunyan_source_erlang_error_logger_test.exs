defmodule BunyanSourceErlangErrorLoggerTest do
  use ExUnit.Case
  doctest BunyanSourceErlangErrorLogger

  test "greets the world" do
    assert BunyanSourceErlangErrorLogger.hello() == :world
  end
end
